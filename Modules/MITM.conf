[MITM]
skip-server-cert-verify = true
h2 = true
hostname = %APPEND% *.figma.*

# Keep ca-p12 and ca-passphrase in your local Surge profile only.
# Do not publish MITM CA private keys in this hosted config.
